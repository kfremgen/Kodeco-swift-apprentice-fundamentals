//: [Previous](@previous)

import Foundation

// : # Challenges

/*: ### Coordinates
 
 Create a constant called coordinates and assign a tuple containing two and three to it.
 */

let coordinates = (2,3)

/*: ###  Named Coordinate
 
 Create a constant called namedCoordinate with a row and column component.
 */

let namedCoordinatese = (row:2,column:3)

/*: ### Summary
 
 Create a constant called number and multiplier.
 Assign  an integer to each constant.
 Create a constant called summary and assign it to a string literal explaining the sum of number and multiplyer.
 */

let number = 5
let multiplier = 10
let summary = "The sum of \(number) and \(multiplier) is \(number * multiplier)"

